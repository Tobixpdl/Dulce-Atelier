import { CakeSlice, Menu, ShoppingBag, X } from 'lucide-react';
export function Header({count,onCart,open,setOpen}:{count:number;onCart:()=>void;open:boolean;setOpen:(v:boolean)=>void}){const links=[['Inicio','inicio'],['Productos','productos'],['Tortas personalizadas','personalizadas'],['Nosotros','nosotros'],['Preguntas frecuentes','faq'],['Contacto','contacto']];const go=(id:string)=>{setOpen(false);document.getElementById(id)?.scrollIntoView({behavior:'smooth'})};return <header><a className="brand" href="#inicio"><CakeSlice aria-hidden="true"/><span>Dulce Atelier<small>pastelería artesanal</small></span></a><nav className={open?'open':''} aria-label="Navegación principal">{links.map(([name,id])=><button key={id} onClick={()=>go(id)}>{name}</button>)}</nav><div className="header-actions"><button className="order-link" onClick={()=>go('productos')}>Hacer pedido</button><button className="cart-button" onClick={onCart} aria-label={`Abrir carrito, ${count} productos`}><ShoppingBag aria-hidden="true"/>{count>0&&<b>{count}</b>}</button><button className="menu-button" onClick={()=>setOpen(!open)} aria-label={open?'Cerrar menú':'Abrir menú'} aria-expanded={open}>{open?<X/>:<Menu/>}</button></div></header>}
