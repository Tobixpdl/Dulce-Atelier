import { Sparkles } from 'lucide-react'; import type { Product } from '../types'; import { money } from '../utils/currency';
export function ProductCard({product,onSelect}:{product:Product;onSelect:(p:Product)=>void}){return <article className="product-card"><button className="product-photo" onClick={()=>onSelect(product)} aria-label={`Ver ${product.name}`}><img src={product.image} alt={product.alt} width="640" height="800" loading="lazy"/>{product.custom&&<span><Sparkles size={14}/> Personalizada</span>}</button><div className="product-content"><p className="eyebrow">{product.category}</p><h3>{product.name}</h3><p>{product.description}</p><div className="product-bottom"><strong>{product.custom?'Desde ':''}{money(product.price)}</strong><button className="card-action" onClick={()=>onSelect(product)} disabled={!product.available}>{product.custom?'Solicitar':'Ver opciones'}</button></div></div></article>}
